---
"@moonshot-ai/kimi-code": minor
---

Switch all entry points (TUI, print mode, ACP, export, login, provider, upgrade, and the VS Code extension) to the v2 engine by default; set KIMI_CODE_ENGINE=v1 to fall back to the legacy engine.
